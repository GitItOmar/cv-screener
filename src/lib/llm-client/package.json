{
  "name": "@screening/llm-client",
  "version": "1.0.0",
  "description": "Unified LLM client supporting multiple providers (OpenAI, DeepSeek)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "build": "echo 'Build not needed for ES modules'",
    "dev": "node --watch examples/basic-usage.js",
    "examples": "node examples/basic-usage.js"
  },
  "dependencies": {
    "openai": "^4.20.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.57.0"
  },
  "keywords": [
    "llm",
    "openai",
    "deepseek",
    "gpt",
    "ai",
    "client"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
